name: edgedb_codegen_tests
publish_to: none
environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  edgedb: any

dev_dependencies:
  build_runner: ^2.2.0

dependency_overrides:
  edgedb:
    path: ../../